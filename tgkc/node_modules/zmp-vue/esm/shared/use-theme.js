import { ref } from 'vue';
import { zmp, zmpready, theme } from './zmp';
export var useTheme = function useTheme() {
  var t = ref(zmp ? theme : null);

  if (!zmp) {
    zmpready(function () {
      t.value = theme;
    });
  }

  return t;
};